# OOP Fundamentals

> TODO: Detailed guide with examples in Java/C++/Python

## Topics to Cover

### Abstraction
- Hiding implementation details, exposing only behavior
- Abstract classes vs interfaces — when to use each
- Examples in Java (abstract class, interface), C++ (pure virtual), Python (ABC)

### Encapsulation
- Bundling data + methods that operate on it
- Access modifiers — private, protected, public (Java/C++ vs Python conventions)
- Getters/setters vs direct access, information hiding

### Inheritance
- is-a relationship — when it's appropriate
- Single vs multiple inheritance (C++ diamond problem, Java interfaces)
- Method overriding and virtual dispatch, fragile base class problem

### Polymorphism
- **Compile-time** (static) — method overloading, operator overloading, templates/generics
- **Runtime** (dynamic) — virtual methods, vtable (C++), interface dispatch
- Parametric polymorphism (generics) — Java `<T>`, C++ templates, Python typing
