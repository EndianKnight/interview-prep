# Databases

> TODO: Detailed guide with examples, diagrams, and interview questions

## Topics to Cover
- SQL vs NoSQL — when to use which
- Relational databases (PostgreSQL, MySQL) — ACID, joins, transactions
- Document stores (MongoDB, DynamoDB) — flexible schema, denormalization
- Key-value stores (Redis, Memcached) — caching, session storage
- Wide-column stores (Cassandra, HBase) — time-series, write-heavy workloads
- Graph databases (Neo4j) — relationship-heavy queries
- **Skip list** — used in Redis sorted sets, LevelDB memtable
- Database indexing — B-tree, hash, composite, covering indexes
- Transactions — isolation levels, 2PC, saga pattern
- Connection pooling and query optimization
