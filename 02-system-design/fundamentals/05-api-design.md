# API Design

> TODO: Detailed guide with examples, diagrams, and interview questions

## Topics to Cover
- REST — resource naming, HTTP methods, status codes, HATEOAS
- GraphQL — schema, queries, mutations, subscriptions, N+1 problem
- gRPC — protobuf, streaming modes, when to use
- REST vs GraphQL vs gRPC comparison matrix
- API versioning strategies (URL path, header, query param)
- Pagination (offset, cursor-based, keyset)
- Rate limiting headers and error responses
- Idempotency and retry safety
- API authentication (API key, OAuth, JWT)
